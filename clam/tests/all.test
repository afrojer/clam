#!/bin/bash
DIR="$( cd "$( dirname "$0" )" && pwd )"
cd $DIR
echo "PWD: $PWD"

CNT=0
PASS=0
for t in *.test; do
	if [ "$(basename $0)" == "$(basename $t)" ]; then continue; fi
	CNT=$(($CNT + 1))
	./$t
	if [ $? -eq 0 ]; then
		PASS=$(($PASS + 1))
	fi
done

echo
echo "PASSED: $PASS / $CNT"
echo

