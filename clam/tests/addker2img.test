#!/bin/bash
TEST_DESC="Test using OrEq to add kernal to an Image"
. _buildup.sh

compile_it
if [ $ERRORS -eq 0 ]; then
	ERRORS=1
	error "Adding a Kernal to an Image by OrEq"
else
	# we purposely succeed b/c this test was
	# designed to fail!
	ERRORS=0
fi

. _breakdown.sh
